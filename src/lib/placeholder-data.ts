import { Escrow, Transaction, Dispute } from '@/types';

// Data is now fetched from the blockchain and is no longer hardcoded.
export const escrows: Escrow[] = [];

export const transactions: Transaction[] = [];

export const disputes: Dispute[] = [];
